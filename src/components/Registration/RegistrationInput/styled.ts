import styled from 'styled-components';

export const BlockInput = styled.div `
position: relative;
`

export const NoValidStyle = styled.span `
    position: absolute;
    bottom: -20px;
    right: 0;
    color: #ff8f8f;
`